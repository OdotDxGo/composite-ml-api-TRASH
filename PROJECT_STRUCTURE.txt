📁 СТРУКТУРА ПРОЕКТА - Hybrid PIRF v3.0
=====================================

Вот как должны быть организованы файлы:

composite-ml-api/                    ← Корневая папка проекта
│
├── app.py                           ← Главный файл API
├── train_models.py                  ← Обучение ML моделей
├── requirements.txt                 ← Python зависимости
├── Procfile                         ← Railway start command
├── railway.toml                     ← Railway build config
├── README.md                        ← Документация
├── .gitignore                       ← Git ignore rules
│
├── material_validator.py            ← Валидация материалов
├── scientific_plotter.py            ← Научные графики
├── pdf_report_generator.py          ← PDF отчёты
├── mechanical_simulator.py          ← Механические симуляции
├── demo.py                          ← Тестирование (опционально)
├── INTEGRATION_GUIDE.md             ← Инструкция (опционально)
│
├── static/                          ← Папка для веб-интерфейса
│   └── index.html                   ← Веб интерфейс
│
├── models/                          ← Папка для ML моделей (создастся автоматически)
│   └── .gitkeep                     ← Чтобы папка была в git
│
├── data/                            ← Папка для данных (создастся автоматически)
│
└── output/                          ← Папка для результатов (создастся автоматически)
    ├── plots/
    └── reports/


📝 ИНСТРУКЦИЯ ПО СБОРКЕ:

1. Создайте папку проекта:
   mkdir composite-ml-api
   cd composite-ml-api

2. Скопируйте все файлы из COMPLETE_PROJECT_V3/ в корень проекта

3. Создайте папку static/ и переместите index.html:
   mkdir static
   mv index.html static/

4. Создайте папку models/ и переместите .gitkeep:
   mkdir models
   mv models.gitkeep models/.gitkeep

5. Готово! Теперь можете:
   - Тестировать локально: python demo.py
   - Загрузить на GitHub
   - Задеплоить на Railway


🚀 БЫСТРЫЙ СТАРТ:

Локально:
  pip install -r requirements.txt
  python train_models.py
  python app.py

Railway:
  git init
  git add .
  git commit -m "Initial commit"
  git push origin main
  (Railway автоматически задеплоит)


✅ ПРОВЕРКА:
После деплоя откройте:
  https://your-url.railway.app/health

Должно показать:
  {
    "status": "healthy",
    "version": "3.0",
    "models_loaded": true,
    "features": [...]
  }
